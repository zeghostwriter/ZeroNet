[package]
name = "zero-router"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
zero-core = { workspace = true }
zero-config = { workspace = true }
zero-net = { workspace = true }
tracing = { workspace = true }
aho-corasick = { workspace = true }
regex = { workspace = true }
ipnet = { workspace = true }
sha2 = { workspace = true }
# Only the blocking pool: asset validation is CPU/disk work that must not run
# on a runtime worker.
tokio = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "net", "time", "io-util"] }
