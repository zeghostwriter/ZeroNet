# ---- Rust / build output ----
/target
**/target/
**/*.rs.bk
*.log

# ---- Fuzzing (regenerable artifacts & corpus) ----
/fuzz/target
/fuzz/artifacts
/fuzz/corpus

# ---- Bundled reference binary / vendored oracles (large, not source) ----
/.xray-oracle
/reference
/local
/assets
/squashfs-root

# ---- Nested / sibling projects with their own repos ----
/Zero-IP-Scanner
# Local study of the BPB Worker Panel (reference only, not shipped)
/bpb-study
# Local dev / test / bootstrap scripts (not shipped in the public repo)
/scripts
# Local git backups made while consolidating into one repo
**/.git-backup
**/*.git.bak

# ---- AI tooling / assistant config (excluded from public repo) ----
.zcodeignore
.aider*
.cursorrules
.cursor/
AGENTS.md
CLAUDE.md
.opencode/

# ---- Android (ZeroNet-Mobile also has its own .gitignore) ----
**/.gradle/
**/.kotlin/
**/build/
**/local.properties
**/*.keystore
**/*.jks
keystore.properties
signing.properties
# Native libs are build outputs of zray-core
ZeroNet-Mobile/app/src/main/jniLibs/

# ---- OS / editor noise ----
.DS_Store
.directory
*.iml
.idea/
